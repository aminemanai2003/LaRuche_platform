apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-financial
  namespace: wealthmesh
  labels:
    app: agent-financial
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-financial
  template:
    metadata:
      labels:
        app: agent-financial
    spec:
      containers:
        - name: agent-financial
          image: wealthmesh/agent-financial:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
          envFrom:
            - configMapRef:
                name: wealthmesh-env
            - secretRef:
                name: wealthmesh-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-financial
  namespace: wealthmesh
spec:
  selector:
    app: agent-financial
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP
