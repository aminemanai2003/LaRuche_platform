apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-docs
  namespace: wealthmesh
  labels:
    app: agent-docs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-docs
  template:
    metadata:
      labels:
        app: agent-docs
    spec:
      containers:
        - name: agent-docs
          image: wealthmesh/agent-docs:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: wealthmesh-env
            - secretRef:
                name: wealthmesh-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-docs
  namespace: wealthmesh
spec:
  selector:
    app: agent-docs
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP
